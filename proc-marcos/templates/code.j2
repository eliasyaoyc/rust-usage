{% for st in structs %}
#[derive(Debug,Default,serde::Serialize,serde::Deserialize)]
pub struct {{ st.name }} {
    {% for field in st.fields %}
       pub {{ field.name }}: {{ field.ty }},
    {% endfor %}
}

{% endfor %}